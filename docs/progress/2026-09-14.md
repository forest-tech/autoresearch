# 2026/09/14

## TODO

- [x] 結果を `~/experiments/autoresearch/EXP_NAME/DATE` に置くようにリファクタリング
  - 元々は `~/projects/autoresearch/results` に置いていた
  - 様々な条件の実験を同時に投げられるようにするため、`git worktree` でブランチをディレクトリとして扱い、複数の条件でジョブを投げられるように
  - すると `results` がバラバラに置かれるようになったため、外に出すように変更
- [x] 前回投げていたジョブ（全件, 1件, 10件履歴を見せる実験）の結果を確認
  - 1件, 10件履歴を見せる実験はOK
  - 全件(baseline)はloopが回らなかったので、修正して再実行
- [x] HPC環境 Miyabi の申請完了
  - 利用登録の手続き中
  - まだ使えない
- [x] `script` を `scripts` に変更
  - シェルスクリプトなどのパスも変更した
- [ ] 全件の履歴を見せる実験(history-all, 旧baseline)の再実行
  - 実行時間が elapse time を超えたため、round 8(round 9 まで)で終了
  - ずっと baseline というブランチがあったため、前の実行での変更が残っていてハイパラ提案に時間がかかった可能性
  - ~~観察すると elapse time = 2 h は結構ギリなので、2 h 30 min に変更して安全に回す~~
    - 時間がギリだったのは↑の可能性もあるので、2 h で回してまたダメだったら変更
- [x] リファクタリングなどのコミットを各ブランチに統合
  - コミットが古くなっていたので、統合するのではなく削除した
- [ ] worker ごとに戦略(prompts)を変える実験の方針に合うようにシェルスクリプトを改造

## NOTES

- `scripts/genkai/` のファイルとかの名前が適当すぎるから、どこかのタイミングで機能ごとに rename するといいかも

## NEXT

- HPC環境 Miyabi で実行する用のスクリプトを作成
