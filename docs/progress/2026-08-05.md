# 2026/08/05

## TODO

- 実験のダミーが動くか確認する
  - 外側の大枠(cpu環境)の動作を確認
  - 内側の実験1個分(gpu環境想定、ダミーはcpu環境でもいい)の動作を確認
  - ダミー全体を動かして動作を確認
  - ここまでできたらslackで報告

## 実験のダミーの構成

- cpu環境の計算ノードに投げるジョブ
  - singularityを展開
  - codexを起動
  - codexにgpu環境の計算ノードに投げるジョブを投げさせ、結果が出たらそれを観察して改善してまたジョブを投げる
- gpu環境の計算ノードに投げるジョブ
  - `singularity exec` で `train.py` を実行

## やったこと

### 実験のダミーの動作確認

#### 外側の大枠の動作を確認する

genkaiの計算ノードa環境で、codexを閉じ込める
