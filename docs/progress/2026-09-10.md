# 2026/09/10

## TODO

- [x] ジョブを投げて終わっていた、`val_loss` を評価指標, round = 10 の実験の結果を確認
  - 正常に動作していた
  - loss: 2.805656 -> 2.777411
- [x] ベースライン（評価指標: val_bpb, default settig）のジョブを実行
- [x] プロンプトを変更したジョブを実行
    - 最近の result を 1, 10 件のみしか与えない例をジョブ投入
- [x] リファクタリング後のコード(今回は `experiment_util.py` のみ)を理解

## NOTES

### 研究リポジトリ内の、結果をプロットしたグラフ.png　の保存方法

現状の構成は以下の通りになっている。

```
.
├── 結果をプロットしたグラフ.png
└── results/      # .gitignore
    └── EXP_DIR/
        ├── baseline
        ├── round_0
        └── results.jsonl
```

しかし、

- 実験数が増えると管理が大変
- git に保存する場合、実験数が増えると圧迫する

という問題があるため構成を変更した。
変更後の構成は以下の通りになっている。合わせて `script/genkai/plot_results_parallel.sh` を変更。

```
.
└── results/      # .gitignore
    └── EXP_DIR/
        ├── baseline
        ├── round_0
        ├── 結果をプロットしたグラフ.png
        └── results.jsonl
```

### 入れ子のシュルスクリプト

- 変数は渡せないので、渡したいなら`$@, $1` などを用いて明示的に渡す必要がある。
- jobのoutファイルを `results/` に置こうと思ってこれを考えたが、面倒そうなのでひとまず実装は後回しにした

### ジョブを複数のブランチから投げたいときの対処法

`git worktree` を使うと、1つのGitリポジトリから複数ブランチを別ディレクトリで同時に扱える。

- 1 job = 1 worktree = 1 branch とすると、複数ジョブを安全に並列実行しやすい。

```
git worktree add ../autoresearch-exp-b -b autoresearch/exp-b HEAD
```

- HEAD を指定すると、現在いるブランチの現在のコミットを起点に新しいブランチを作成する。
- master を起点にしたい場合は、最後を master に変更する。

```
git worktree add ../autoresearch-exp-b -b autoresearch/exp-b master
```

[!] `script/genkai/parallel_loop_for_codex.sh, loop_for_codex.sh` を投げる前に、`WORKDIR` を現在のものに変更する！

## NEXT

- baseline, history-1,10 の結果を確認
- MTG資料を作成
