# 2026/08/28

## 今日やること

## やったこと

- 実験フローの再構築
  - 改善してたら採用、悪化してたら棄却のシステムができてなかった

## 実験フロー

```text
人間
 │
 │ あらかじめ autoresearch/* ブランチを作る
 ▼
loop_for.sh
 │
 ├─ ① baseline train.py を実行
 │      ↓
 │    train.log
 │      ↓
 │    シェルが val_bpb を抽出
 │      ↓
 │    results.tsv に記録
 │
 ├─ ② Codex
 │      ├─ results.jsonl を読む
 │      ├─ 過去のログ・diffを見る
 │      └─ train.py だけ変更
 │
 ├─ ③ シェルが変更diffを保存
 │
 ├─ ④ train.py を実行
 │
 ├─ ⑤ シェルが結果を解析
 │
 ├─ 改善 → keep + git commit
 │
 └─ 悪化 → discard + train.py を元に戻す
        │
        └────────────→ 次のCodex
```

## プロンプト

原文(LLM作成)：

```text
You are preparing exactly one candidate experiment for an autonomous
LLM training loop on HPC environment.

Current iteration: ${ITER}
Current accepted commit: ${BASE_COMMIT}

Primary metric:
val_bpb (lower is better)

Current experiment run directory:
${RUN_ROOT_REL}

Current experiment history:
${RESULT_REL}

The shell script owns:

- training execution
- ${RESULT_REL}
- keep/discard decisions
- git commits
- experiment artifact management

Your task is ONLY to choose the next experiment and edit train.py.

Instructions:

- Read README.md, prepare.py, train.py, and ${RESULT_REL}.
- Inspect artifacts from the current run under ${RUN_ROOT_REL}/ when useful.
- Use previous keep/discard results and recorded hyperparameters to avoid repeating unsuccessful experiments.
- Compare the current accepted configuration with previous candidate configurations when deciding the next experiment.
- Choose one change that has a plausible chance to lower val_bpb.
- Prefer one-factor-at-a-time and small/local changes initially.
- Avoid repeating previous experiments.
- Edit ONLY train.py.
- Do NOT run uv run train.py.
- Do NOT edit prepare.py.
- Do NOT edit program.md.
- Do NOT edit ${RESULT_REL}.
- Do NOT edit files under results/.
- Do NOT edit scripts.
- Do NOT modify .git.
- Do NOT run git commit/reset/checkout/restore.
- Leave train.py runnable as-is.

Final response:
output only a short one-line description of the experiment.
Do not use tabs.
```

和訳：

```text
あなたは、HPC環境上で動作する自律的なLLM学習ループのために、**ちょうど1つの候補実験**を準備しています。

現在のイテレーション: ${ITER}
現在採用されているコミット: ${BASE_COMMIT}

主要評価指標:
val_bpb（低いほど良い）

現在の実験実行ディレクトリ:
${RUN_ROOT_REL}

現在の実験履歴:
${RESULT_REL}

以下の処理はシェルスクリプトが担当します。

* 学習の実行
* ${RESULT_REL} の管理
* keep/discard の判定
* gitコミット
* 実験成果物の管理

あなたのタスクは、**次に行う実験を1つ選び、train.pyを編集することだけ**です。

指示:

* README.md、prepare.py、train.py、${RESULT_REL} を読むこと。
* 必要に応じて、現在の実験の成果物を ${RUN_ROOT_REL}/ 以下から確認すること。
* 過去のkeep/discard結果と記録されたハイパーパラメータを利用し、失敗した実験を繰り返さないこと。
* 次の実験を決める際は、現在採用されている設定と過去の候補設定を比較すること。
* val_bpbを改善できる可能性がある変更を1つ選ぶこと。
* 特に初期段階では、一度に1つの要素だけを変更する実験と、小規模・局所的な変更を優先すること。
* 過去に行った実験を繰り返さないこと。
* 編集するのはtrain.pyだけにすること。
* `uv run train.py` を実行しないこと。
* prepare.pyを編集しないこと。
* program.mdを編集しないこと。
* ${RESULT_REL} を編集しないこと。
* results/ 以下のファイルを編集しないこと。
* scriptsを編集しないこと。
* .gitを変更しないこと。
* `git commit`、`git reset`、`git checkout`、`git restore` を実行しないこと。
* train.pyは、そのまま実行可能な状態にしておくこと。

最終応答:
実験内容を簡潔に説明した1行のみを出力すること。
タブ文字は使用しないこと。

```

## 結果を記録するjsonl(results.jsonl) について

以下の形式で記録することとする。

```jsonl
{
  "iteration": 3,
  "status": "keep",
  "base_commit": "123abcd",
  "commit": "456efgh",
  "description": "reduce matrix learning rate",
  "hyperparameters": {
    "ASPECT_RATIO": 64,
    "HEAD_DIM": 128,
    "WINDOW_PATTERN": "SSSL",
    "TOTAL_BATCH_SIZE": 524288,
    "EMBEDDING_LR": 0.6,
    "UNEMBEDDING_LR": 0.004,
    "MATRIX_LR": 0.03,
    "SCALAR_LR": 0.5,
    "WEIGHT_DECAY": 0.2,
    "ADAM_BETAS": [0.8, 0.95],
    "WARMUP_RATIO": 0.0,
    "WARMDOWN_RATIO": 0.5,
    "FINAL_LR_FRAC": 0.0,
    "DEPTH": 8,
    "DEVICE_BATCH_SIZE": 128
  },
  "fixed_config": {
    "MAX_SEQ_LEN": 2048,
    "TIME_BUDGET": 300
  },
  "model_config": {
    "sequence_len": 2048,
    "n_layer": 8,
    "n_head": 4,
    "n_embd": 512
  },
  "metrics": {
    "val_bpb": 0.991832,
    "training_seconds": 300.4,
    "total_seconds": 315.2,
    "peak_vram_mb": 45012.3,
    "mfu_percent": 42.1,
    "total_tokens_M": 123.4,
    "num_steps": 235,
    "num_params_M": 162.5
  },
  "artifacts": {
    "log": "results/iter_003/train.log",
    "patch": "results/iter_003/change.patch",
    "config": "results/iter_003/config.json"
  }
}
```
